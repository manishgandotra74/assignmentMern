
const initialState = {
    saveusermessage:"",
};
export default function authorReducer(state = initialState, action) {
    switch (action.type) {
              default:
            return state;
    }
}